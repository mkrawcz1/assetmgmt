services:
  assetmgmt:
    build: .
    container_name: assetmgmt
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      APP_UID: ${APP_UID:-1000}
      APP_GID: ${APP_GID:-1000}
    volumes:
      - ./data:/data
    restart: unless-stopped
